// Deixe vazio quando frontend e servidor Node usam o mesmo domínio.
// Em hospedagens separadas use, por exemplo: https://api.seujogo.com
window.NEON_API_BASE = window.NEON_API_BASE || window.NEON_PATH_BACKEND || "";
